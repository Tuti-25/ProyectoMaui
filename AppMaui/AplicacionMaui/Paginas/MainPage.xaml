<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AplicacionMaui.MainPage">

    <FlexLayout Direction="Column">
        <HorizontalStackLayout Padding="10" 
                               FlexLayout.Basis="120">
            <Button x:Name="btnAgregar" Text="Agregar" Margin="0,0,10,0" ></Button>
            <Button x:Name="btnVerUsuarios" Text="Ver usuarios"></Button>
        </HorizontalStackLayout>
        <ListView x:Name="UsuarioListView" FlexLayout.Grow="1" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <VerticalStackLayout Padding="5">
                            <Label Text="{Binding NombreUsuario}"
                                   FontSize="17"
                                   FontAttributes="Bold">
                            </Label>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0"
                                       Text="{Binding IdUsuario}">
                                </Label>
                                <Label Grid.Column="2"
                                       Text="{Binding ApellidoUsuario}"
                                       FontAttributes="Bold"
                                       TextColor="Red">

                                </Label>
                            </Grid>
                        </VerticalStackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </FlexLayout>

</ContentPage>
