using Microsoft.Maui.Controls;

namespace MenuView
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();  // Inicializa los componentes de Shell
        }
    }
}
